const PREFIX = 'react-easy-auto-cofig-table';

export const ErrorMessages = {
  InvalidFilterIndex: `[${PREFIX}]: Array of string needed when column is multiple.`,
  InvalidValue: `[${PREFIX}]: Invalid value!`,
};
